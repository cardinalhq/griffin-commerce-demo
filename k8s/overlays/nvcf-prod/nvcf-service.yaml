apiVersion: v1
kind: Service
metadata:
  name: nvcf-faults
  labels:
    app: nvcf
    purpose: nvcf-telemetry-simulator
spec:
  type: ClusterIP
  selector:
    app: nvcf
  ports:
    - name: faults
      port: 9998
      targetPort: faults
      protocol: TCP
